<Page
    x:Class="Kopra.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kopra"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="Light">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="KOPRA" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="Ustawienia" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <StackPanel>
                <Button x:Name="button" Content="Odśwież &quot;Web Key&quot;" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Top" Width="342" Height="89" Click="button_Click"/>
                <CheckBox HorizontalAlignment="Center" x:Uid="CkeckBoxWeryfikacjaPracodawcy" Content="Notyfikacje PUSH" IsChecked="{Binding WeryfikacjaPracodawcy, Mode=TwoWay}"/>
                <Button x:Name="logoutButton" Content="Wyloguj"  Margin="0,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Center" Click="logoutButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
